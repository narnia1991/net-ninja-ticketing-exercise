import CreateForm from "./CreateForm";

export default function CreteTicket() {
  return (
    <main>
      <h2 className="text-primary text-center">
        Add a new Ticket
      </h2>
      <CreateForm />
    </main>
  )
}
